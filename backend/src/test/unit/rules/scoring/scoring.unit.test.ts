/**
 * Scoring Rules Unit Test
 *
 * Tests individual scoring rules in isolation.
 *
 * Responsibilities:
 * - Create controlled test scenarios (adopter + pet)
 * - Execute single scoring rules
 * - Verify score outputs match expected behavior
 *
 * This test ensures correctness of scoring logic.
 * It does NOT involve multiple rules or full matching flow.
 */