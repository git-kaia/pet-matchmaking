// Handles:
// All reject logic (rules table)


// isRejected(user, pet) {
//   if (user.daily_care_time === 0) return true;

//   if (
//     pet.requires_bird_partner &&
//     user.willingness_multiple_birds === "low"
//   ) return true;

//   return false;
// }